import { Route } from '@angular/router';
import { InsertUserComponent } from './index';

export const InsertUserRoutes: Route[] = [
    {
        path: 'insertUser',
        component: InsertUserComponent
    }
];
